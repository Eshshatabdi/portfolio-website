import React from 'react';
import Navbar from '../../Shared/Navbar';
import Header from '../Header';

const Home = () => {
    return (
        <div>
            <Navbar></Navbar>
            <Header></Header>

        </div>
    );
};

export default Home;